{
  "name": "@live-tab-mirror/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@live-tab-mirror/shared": "0.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260629.1",
    "wrangler": "4.105.0"
  }
}
